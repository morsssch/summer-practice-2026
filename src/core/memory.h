#pragma once

#include "types.h"

// ── Структуры-заглушки ────────────────────────────────────────────────────
// Пока пустые — будут наполняться по мере разработки.

// Живёт с запуска до закрытия игры
struct GlobalState {
    // renderer, atlas, audio engine — добавим на этапе рендера
};

// Живёт с начала игровой сессии до выхода в меню
struct GameState {
    int  currentScreen = 0;
    int  collectedItems = 0;
    Vec2 playerPos;
    // сохранение прогресса — добавим позже
};

// Живёт пока игрок на текущем экране, вайпается при переходе
struct ScreenState {
    // враги, трава, лианы — добавим на этапах 6-7
};

{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build & Run",
            "type": "shell",
            "command": "cd build && cmake .. -DCMAKE_BUILD_TYPE=Debug > /dev/null && make && ./Platformer",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "panel": "dedicated",
                "clear": true
            }
        }
    ]
}
